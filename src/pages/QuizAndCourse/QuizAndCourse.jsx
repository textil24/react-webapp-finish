import React from 'react';
import Study from "../../components/Study/Study";

const QuizAndCourse = ({ title, sort }) => {
    return <Study title={title} sort={sort}/>
};

export default QuizAndCourse;